server:
  port: 8002

spring:
  application:
    name: lagou-edu-course
  cloud:
    config:
      uri: http://192.168.100.100:8090
      label: master
      profile: dev
      name: lagou-edu-course

eureka:
  client:
    service-url:
      defaultZone: http://192.168.100.100:8761/eureka/

  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}

